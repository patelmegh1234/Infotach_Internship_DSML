# Base URL for the AcousticSpace backend API
# When using Vite proxy (default), leave this empty or set to ''.
# If the backend is hosted elsewhere, provide the full URL:
# Example: VITE_API_BASE_URL=https://api.acousticspace.example.com
VITE_API_BASE_URL=

